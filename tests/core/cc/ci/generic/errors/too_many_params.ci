//@expected_compiler_error :12:15: \e[31merror\e[0m[0192]: the number of generic params does not correspond to its declaration
//@expected_compiler_error \e[34m12 |\e[0m \tstruct Pair.[int, char, int] p;

#include <helper.h>

struct Pair.[@A, @B] {
	@A a;
	@B b;
};

int main() {
	struct Pair.[int, char, int] p;

	return 0;
}
