//@expected_stdout PASS\n

#include <helper.h>

enum Letter {
	A = 0,
	B = 1,
	C = 2,
	D = 3,
	E = 10,
	F,
	G = A + 12
};

enum Animal {
	Cat,
	Dog
};

int main() {
	if (G == 12) {
		printf("PASS\n");
	}
}
