//@compile_options --method-convention $type_$name
//@expected_bin_stdout 3\n

#include <helper.h>

typedef struct Vec {
	size_t len;
} Vec;

size_t Vec_size(const Vec *self)
{
	return self->len;
}

int main()
{
	Vec v;
	Vec *p = &v;

	v.len = 3;

	printf("%zu\n", p->size());

	return 0;
}
