//@compile_options --method-convention $type_$name
//@expected_bin_stdout 5\n

#include <helper.h>

typedef char *str;

size_t str_len(str s)
{
	return 5;
}

int main()
{
	str s = "Hello";

	printf("%zu\n", s.len());

	return 0;
}
