//@compile_options --method-convention $type_$name
//@expected_bin_stdout 7\n

#include <helper.h>

typedef struct Vec {
	size_t len;
} Vec;

size_t Vec_size(const Vec *self)
{
	return self->len;
}

int main()
{
	Vec v;

	v.len = 7;

	printf("%zu\n", v.size());

	return 0;
}
