//@expected_bin_stdout PASS: 20\nPASS: 40\nPASS: 100\n

#include <helper.h>

#define MERGE(a, b) a##b
#define MERGE2(a, b, c) a##b##c
#define MERGE3(a, b, c, d) a##b##c##d

int main() {
	int MERGE(if, 0) = 20;
	MERGE2(l, o, ng) xx = 40;
	MERGE3(l, o, n, g) abc = 100;

	printf("PASS: %d\n", if0);
	printf("PASS: %d\n", xx);
	printf("PASS: %d\n", abc);
}
