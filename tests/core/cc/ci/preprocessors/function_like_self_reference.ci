//@expected_bin_stdout 5\n

#include <helper.h>

int add_one(int x) {
	return x + 1;
}

/* A macro wrapping the function it is named after: the call in its own
   expansion must reach the function, not the macro again. */
#define add_one(x) add_one(x)

int main() {
	printf("%d\n", add_one(4));
}
