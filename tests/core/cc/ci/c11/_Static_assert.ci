//@compile_options -s c11
//@expected_bin_stdout ok\n

#include <helper.h>

_Static_assert(1 + 1 == 2, "at file scope");

int main() {
	_Static_assert(2 * 3 == 6, "at block scope");

	if (1) {
		_Static_assert(4 - 1 == 3, "in a nested block");
	}

	puts("ok");
}
