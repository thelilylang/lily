//@compile_options -s c11
//@expected_bin_stdout 1:1\n

#include <helper.h>

struct S
{
	int tag;
	union
	{
		int i;
		char c;
	};
};

int main() {
	struct S s;

	s.tag = 1;
	s.i = 1;

	printf("%d:%d\n", s.tag, s.i);
}
