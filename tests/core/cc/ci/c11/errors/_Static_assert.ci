//@compile_options -s c11
//@expected_compiler_error :7:1: \e[31merror\e[0m[0193]: one is not two
//@expected_compiler_error \e[34m7 |\e[0m _Static_assert(1 == 2, "one is not two");

#include <helper.h>

_Static_assert(1 == 2, "one is not two");

int main() {
	return 0;
}
