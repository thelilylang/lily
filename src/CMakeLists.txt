if(LILY_WASM)
  # The wasm build is limited to what `cic` needs. `lily`/`lilyc` are left out
  # because they link LLVM, and no native binary is produced at all.
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/base)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/cli/cic)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/command/cic)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/core/cc)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/core/cpp)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/core/lily/diagnostic)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/core/shared)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/wasm)
else()
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/base)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/bin)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/cli)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/command)
  add_subdirectory(${CMAKE_SOURCE_DIR}/src/core)
endif()
